# AgentScope - 多智能体框架
agentscope>=0.1.0

# 数据处理
numpy>=1.24.0

# 结构化输出
pydantic>=2.0.0

# DashScope API (阿里云通义千问)
dashscope>=1.14.0

# OpenAI API (可选 - 用于智谱AI等兼容接口)
openai>=1.0.0

# Ollama (可选 - 用于本地模型)
# ollama>=0.1.0

# 其他依赖
typing-extensions>=4.5.0

# FastAPI server (frontend integration)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
